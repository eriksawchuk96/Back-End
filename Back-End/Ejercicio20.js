const palabras = [
  "sol",
  "computadora",
  "mar",
  "programación",
  "luz",
  "televisor",
];
for (let i = 0; i < palabras.length; i++) {
  if (palabras[i].length >= 5) {
    console.log(palabras[i]);
  }
}
